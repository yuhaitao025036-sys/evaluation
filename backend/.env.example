# DUCC Evaluation System - Environment Variables (示例配置)
#
# ⚠️ 使用步骤:
#   1. 复制此文件: cp .env.example .env
#   2. 修改 .env 中的配置为你的实际值
#   3. .env 文件已被 .gitignore 保护,不会提交到 Git

# ============================================================================
# 数据库配置
# ============================================================================
# PostgreSQL 连接字符串
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://ducc_user:YOUR_PASSWORD_HERE@localhost:5432/ducc_evaluation

# ============================================================================
# Redis 配置
# ============================================================================
REDIS_URL=redis://localhost:6379/0

# ============================================================================
# 应用配置
# ============================================================================
# 调试模式 (生产环境设置为 False)
DEBUG=True

# API 服务端口
API_PORT=8000

# ============================================================================
# 文件路径配置
# ============================================================================
# ⚠️ 重要: 变量名需与 config.py 中的字段名完全一致

# 数据目录
DATA_DIR=/path/to/evaluation/data

# 数据集目录
DATASETS_DIR=/path/to/evaluation/data/datasets

# 脚本目录
SCRIPTS_DIR=/path/to/evaluation/data/scripts

# 输出目录
OUTPUTS_DIR=/path/to/evaluation/data/outputs

# ============================================================================
# Worker 配置
# ============================================================================
# Worker 标识 (用于区分多个 Worker)
WORKER_ID=worker-1

# RQ 队列名称
RQ_QUEUE_NAME=ducc_tasks

# ============================================================================
# CORS 配置
# ============================================================================
# 允许的前端地址 (用逗号分隔)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

